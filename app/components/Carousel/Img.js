import styled from 'styled-components';

import NormalImg from 'components/Img';

const Img = styled(NormalImg)`
  max-width: 100%;
  max-height: 100%;
  margin-left: auto;
  margin-right: auto;
`;

export default Img;
